DEBUG: Leave run
{{ $db := or ( dbGet .User.ID "member" ).Value (
    sdict
    "OnServer" false
    "Agreed" false
    "Staff" false
) }}
{{ $db.Set "OnServer" false }}
{{ dbSet .User.ID "member" $db }}
DEBUG: Leave complete